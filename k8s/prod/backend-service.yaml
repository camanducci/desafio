apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
    environment: prod
spec:
  type: NodePort
  selector:
    app: backend
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30101
    protocol: TCP
    name: http
